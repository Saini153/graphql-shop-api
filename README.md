# graphql-shop-api
This project is a Graph implementation of a backend system capable of performing full CRUD operations (Create, Read, Update, Delete) on a Shop model.  The API is designed to efficiently handle data queries by allowing clients to request only the required fields using GraphQL, improving performance and flexibility compared to traditional REST APIs.
